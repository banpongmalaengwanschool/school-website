

## 📁 โครงสร้างโฟลเดอร์

```
school-website/
├── index.html              # หน้าแรก
├── news.html              # หน้าข่าวสาร
├── downloads.html         # หน้าดาวโหลด
├── staff.html             # หน้าครูและบุคลากร
├── students.html          # หน้านักเรียน
│
├── css/
│   ├── style.css          # CSS หลัก
│   └── news.css           # CSS สำหรับหน้าข่าวสาร
│
├── js/
│   ├── script.js          # JavaScript หลัก
│   └── news.js            # JavaScript สำหรับข่าวสาร
│
├── data/
│   ├── news.json          # ข้อมูลข่าวสาร (แก้ไขที่นี่)
│   └── downloads.json     # ข้อมูลไฟล์ดาวโหลด (แก้ไขที่นี่)
│
├── images/
│   ├── logo.jpg           # โลโก้โรงเรียน
│   └── school-entrance.png # ภาพหน้าโรงเรียน
│
├── news-images/           # เก็บรูปภาพข่าวสาร
├── downloads/             # เก็บไฟล์ดาวโหลด
├── files/                 # เก็บไฟล์อื่นๆ
└── README.md              # ไฟล์นี้
```

---

## 🔧 วิธีการอัปเดตข่าวสาร

### 1. **อัปเดตข่าวสารใหม่**

แก้ไขไฟล์ `data/news.json` ด้วย Text Editor (เช่น Notepad, VS Code)

**ตัวอย่าง:**
```json
{
  "news": [
    {
      "id": 1,
      "title": "ชื่อข่าวสาร",
      "category": "ประกาศ",
      "date": "2025-02-20",
      "description": "รายละเอียดข่าวสาร...",
      "image": "news-images/image-name.jpg",
      "files": [
        {
          "id": 1,
          "name": "ชื่อไฟล์.pdf",
          "url": "downloads/file-name.pdf",
          "size": "2.5 MB",
          "type": "pdf"
        }
      ]
    }
  ]
}
```

**ขั้นตอน:**
1. เปิดไฟล์ `data/news.json`
2. เพิ่มข่าวสารใหม่ลงในอาร์เรย์ `news`
3. บันทึกไฟล์
4. Push ไปยัง GitHub

---

## 📥 วิธีการเพิ่มรูปภาพ

1. **เตรียมรูปภาพ** (ขนาดแนะนำ: 1200x600px หรือ 800x400px)
2. **บันทึกรูปภาพ** ในโฟลเดอร์ `news-images/`
3. **ใช้ชื่อไฟล์ที่เข้าใจง่าย** เช่น `news-1.jpg`, `event-2025.jpg`
4. **อัปเดต `news.json`** โดยใส่ URL รูปภาพ เช่น `"image": "news-images/news-1.jpg"`

---

## 📥 วิธีการเพิ่มไฟล์ดาวโหลด

### 1. **วิธีที่ 1: เพิ่มไฟล์ในข่าวสาร**

แก้ไขไฟล์ `data/news.json` และเพิ่มไฟล์ในส่วน `files`:

```json
"files": [
  {
    "id": 1,
    "name": "ชื่อไฟล์.pdf",
    "url": "downloads/file-name.pdf",
    "size": "2.5 MB",
    "type": "pdf"
  }
]
```

### 2. **วิธีที่ 2: เพิ่มไฟล์ในหน้าดาวโหลด**

แก้ไขไฟล์ `data/downloads.json`:

```json
{
  "id": 1,
  "category": "แบบฟอร์ม",
  "title": "ชื่อไฟล์",
  "description": "รายละเอียด",
  "file": {
    "name": "file-name.pdf",
    "url": "downloads/file-name.pdf",
    "size": "2.5 MB",
    "type": "pdf"
  },
  "date": "2025-02-20"
}
```

**ประเภทไฟล์ที่รองรับ:**
- `pdf` - ไฟล์ PDF
- `word` - ไฟล์ Word (.docx, .doc)
- `excel` - ไฟล์ Excel (.xlsx, .xls)
- `zip` - ไฟล์ ZIP

---

## 📝 ตัวอย่างการแก้ไข

### เพิ่มข่าวสารใหม่

```json
{
  "id": 4,
  "title": "ประกาศปิดเรียนช่วงปิดเทอม",
  "category": "ประกาศ",
  "date": "2025-02-21",
  "description": "โรงเรียนจะปิดเรียนช่วงปิดเทอม ตั้งแต่วันที่ 1 มีนาคม ถึง 15 มีนาคม 2568",
  "image": "news-images/closure-2568.jpg",
  "files": []
}
```

### เพิ่มไฟล์ดาวโหลดใหม่

```json
{
  "id": 5,
  "category": "เอกสาร",
  "title": "แบบรายงานผลการเรียน",
  "description": "แบบรายงานผลการเรียนสำหรับครูกรอก",
  "file": {
    "name": "report-form.xlsx",
    "url": "downloads/report-form.xlsx",
    "size": "0.5 MB",
    "type": "excel"
  },
  "date": "2025-02-21"
}
```

---

## 🚀 การอัปโหลดไปยัง GitHub Pages

### 1. **สร้าง Repository**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/school-website.git
git branch -M main
git push -u origin main
```

### 2. **ตั้งค่า GitHub Pages**
- ไปที่ Settings → Pages
- เลือก Main branch
- เลือก Root folder
- บันทึก

### 3. **เว็บไซต์จะเผยแพร่ที่:**
```
https://username.github.io/school-website/
```

---

## 🎨 การปรับแต่งสี

แก้ไขไฟล์ `css/style.css` ส่วน `:root`:

```css
:root {
    --primary-pink: #E91E8C;      /* สีชมพูหลัก */
    --dark-pink: #C71585;         /* สีชมพูเข้ม */
    --light-pink: #FF69B4;        /* สีชมพูอ่อน */
    --black: #1a1a1a;             /* สีดำ */
}
```

---

## 📋 Checklist สำหรับการใช้งาน

- [ ] แก้ไขข้อมูลโรงเรียนในไฟล์ `index.html`
- [ ] เพิ่มรูปภาพโลโก้ในโฟลเดอร์ `images/`
- [ ] เพิ่มรูปภาพข่าวสารในโฟลเดอร์ `news-images/`
- [ ] อัปเดตข่าวสารในไฟล์ `data/news.json`
- [ ] อัปเดตไฟล์ดาวโหลดในไฟล์ `data/downloads.json`
- [ ] เพิ่มไฟล์ดาวโหลดจริงในโฟลเดอร์ `downloads/`
- [ ] ทดสอบเว็บไซต์ในเบราว์เซอร์
- [ ] Push ไปยัง GitHub

---

## ⚠️ หมายเหตุสำคัญ

1. **ชื่อไฟล์:** ใช้ชื่อไฟล์ที่ไม่มีช่องว่าง ใช้ `-` หรือ `_` แทน
2. **เข้ารหัส:** ใช้ UTF-8 encoding สำหรับไฟล์ JSON
3. **ไฟล์ JSON:** ตรวจสอบให้แน่ใจว่า JSON ถูกต้องด้วย [JSONLint](https://jsonlint.com/)
4. **ขนาดไฟล์:** ไฟล์ที่อัปโหลดควรไม่เกิน 100 MB

---

## 📞 ติดต่อและสนับสนุน

หากมีคำถามหรือปัญหา โปรดติดต่อ:
- **อีเมล:** pmaelngwan@gmail.com
- **ที่อยู่:** หมู่ที่ 5 ตำบลโคกกรวด อำเภอเมืองนครราชสีมา จังหวัดนครราชสีมา 30280

---