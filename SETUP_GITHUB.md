

## 🎯 วิธีที่ 1: ใช้ GitHub Web Interface (ง่ายที่สุด - แนะนำ)

### ขั้นตอนที่ 1: สร้าง GitHub Account

1. ไปที่ [GitHub.com](https://github.com)
2. คลิก "Sign up"
3. ใส่อีเมล ชื่อผู้ใช้ รหัสผ่าน
4. ทำการยืนยันอีเมล

### ขั้นตอนที่ 2: สร้าง Repository

1. เข้าสู่ GitHub
2. คลิก "+" ที่มุมบนขวา
3. เลือก "New repository"
4. ตั้งชื่อ Repository: `school-website`
5. เลือก "Public"
6. คลิก "Create repository"

### ขั้นตอนที่ 3: อัปโหลดไฟล์

1. เข้าไปในโฟลเดอร์ Repository ที่เพิ่งสร้าง
2. คลิก "Add file" → "Upload files"
3. ลากไฟล์ทั้งหมดจากโฟลเดอร์ `school-website` มาวาง
4. ใส่ข้อความ "Initial commit"
5. คลิก "Commit changes"

### ขั้นตอนที่ 4: ตั้งค่า GitHub Pages

1. ไปที่ "Settings" ของ Repository
2. ไปที่ "Pages" (ด้านซ้าย)
3. ใน "Build and deployment" เลือก:
   - Source: "Deploy from a branch"
   - Branch: "main" และ "/(root)"
4. คลิก "Save"

### ขั้นตอนที่ 5: เข้าชมเว็บไซต์

- เว็บไซต์จะเผยแพร่ที่ `https://{username}.github.io/school-website/`
- อาจใช้เวลา 1-2 นาทีในการเผยแพร่

---

## 🎯 วิธีที่ 2: ใช้ Git (สำหรับผู้ที่ใช้ Command Line)

### ขั้นตอนที่ 1: ติดตั้ง Git

**Windows:**
1. ดาวโหลด Git จาก [git-scm.com](https://git-scm.com)
2. ติดตั้งตามขั้นตอน

**Mac:**
```bash
brew install git
```

**Linux:**
```bash
sudo apt-get install git
```

### ขั้นตอนที่ 2: ตั้งค่า Git

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### ขั้นตอนที่ 3: สร้าง Repository บน GitHub

1. ไปที่ [GitHub.com](https://github.com)
2. คลิก "+" → "New repository"
3. ตั้งชื่อ `school-website`
4. เลือก "Public"
5. คลิก "Create repository"

### ขั้นตอนที่ 4: Clone Repository

```bash
git clone https://github.com/{username}/school-website.git
cd school-website
```

### ขั้นตอนที่ 5: คัดลอกไฟล์

1. คัดลอกไฟล์ทั้งหมดจากโฟลเดอร์ `school-website` ไปยังโฟลเดอร์ที่เพิ่งสร้าง

### ขั้นตอนที่ 6: Commit และ Push

```bash
# เพิ่มไฟล์ทั้งหมด
git add .

# บันทึกการเปลี่ยนแปลง
git commit -m "Initial commit"

# อัปโหลดไปยัง GitHub
git push origin main
```

### ขั้นตอนที่ 7: ตั้งค่า GitHub Pages

1. ไปที่ Repository Settings
2. ไปที่ "Pages"
3. เลือก "Deploy from a branch"
4. เลือก "main" branch
5. คลิก "Save"

### ขั้นตอนที่ 8: เข้าชมเว็บไซต์

- เว็บไซต์จะเผยแพร่ที่ `https://{username}.github.io/school-website/`

---

## 🔄 วิธีการอัปเดตหลังจากเปลี่ยนแปลง

### ใช้ GitHub Web Interface

1. ไปที่ Repository ของคุณ
2. คลิก "Add file" → "Upload files"
3. เลือกไฟล์ที่เปลี่ยนแปลง
4. คลิก "Commit changes"

### ใช้ Git Command Line

```bash
# เข้าไปในโฟลเดอร์โครงการ
cd school-website

# เพิ่มไฟล์ที่เปลี่ยนแปลง
git add .

# บันทึกการเปลี่ยนแปลง
git commit -m "Update news and files"

# อัปโหลดไปยัง GitHub
git push origin main
```

**หมายเหตุ:** เว็บไซต์จะอัปเดตโดยอัตโนมัติในเวลา 1-2 นาที

---

## 🎯 วิธีที่ 3: ใช้ GitHub Desktop (GUI)

### ขั้นตอนที่ 1: ติดตั้ง GitHub Desktop

1. ดาวโหลด [GitHub Desktop](https://desktop.github.com)
2. ติดตั้งตามขั้นตอน

### ขั้นตอนที่ 2: เข้าสู่ GitHub

1. เปิด GitHub Desktop
2. คลิก "File" → "Options"
3. เข้าสู่ GitHub Account

### ขั้นตอนที่ 3: Clone Repository

1. คลิก "File" → "Clone Repository"
2. ใส่ URL: `https://github.com/{username}/school-website.git`
3. เลือกโฟลเดอร์ที่ต้องการ
4. คลิก "Clone"

### ขั้นตอนที่ 4: คัดลอกไฟล์

1. คัดลอกไฟล์ทั้งหมดจากโฟลเดอร์ `school-website` ไปยังโฟลเดอร์ที่เพิ่งสร้าง

### ขั้นตอนที่ 5: Commit และ Push

1. เปิด GitHub Desktop
2. ใส่ข้อความ "Initial commit"
3. คลิก "Commit to main"
4. คลิก "Push origin"

### ขั้นตอนที่ 6: ตั้งค่า GitHub Pages

1. ไปที่ Repository Settings
2. ไปที่ "Pages"
3. เลือก "Deploy from a branch"
4. เลือก "main" branch
5. คลิก "Save"

---

## 📋 Checklist สำหรับการอัปโหลด

- [ ] สร้าง GitHub Account
- [ ] สร้าง Repository `school-website`
- [ ] อัปโหลดไฟล์ทั้งหมด
- [ ] ตั้งค่า GitHub Pages
- [ ] รอ 1-2 นาทีให้ GitHub ประมวลผล
- [ ] เข้าชมเว็บไซต์ที่ `https://{username}.github.io/school-website/`
- [ ] ทดสอบว่าทุกหน้าทำงานได้

---

## 🔧 การแก้ไขปัญหา

### ❓ Q: GitHub Pages ไม่แสดง

**A:** ตรวจสอบการตั้งค่า

1. ไปที่ Repository Settings → Pages
2. ตรวจสอบว่า Source เป็น "Deploy from a branch"
3. ตรวจสอบว่า Branch เป็น "main"
4. รอ 1-2 นาที

### ❓ Q: ไฟล์ไม่อัปเดต

**A:** ลองรีเฟรชแบบบังคับ

1. กด Ctrl+F5 (Windows) หรือ Cmd+Shift+R (Mac)
2. ลบ Cache ของเบราว์เซอร์
3. ลองใช้เบราว์เซอร์อื่น

### ❓ Q: URL ไม่ถูกต้อง

**A:** ตรวจสอบชื่อ Repository

- ถ้าชื่อ Repository คือ `school-website` URL จะเป็น:
  ```
  https://{username}.github.io/school-website/
  ```

- ถ้าต้องการ URL ที่สั้นกว่า ให้ตั้งชื่อ Repository เป็น `{username}.github.io`:
  ```
  https://{username}.github.io/
  ```

---

## 💡 เคล็ดลับ

1. **ใช้ GitHub Web Interface** ถ้าคุณไม่ชินกับ Command Line
2. **ใช้ Git** ถ้าคุณต้องการการควบคุมที่ดีขึ้น
3. **ใช้ GitHub Desktop** ถ้าคุณต้องการ GUI ที่ง่าย
4. **ทดสอบเว็บไซต์ในเครื่องก่อน** ก่อนอัปโหลดไป GitHub

---

## 📞 ติดต่อและสนับสนุน

หากมีคำถามหรือปัญหา โปรดติดต่อ:
- **อีเมล:** pmaelngwan@gmail.com
- **ที่อยู่:** หมู่ที่ 5 ตำบลโคกกรวด อำเภอเมืองนครราชสีมา จังหวัดนครราชสีมา 30280

